# Backend API Endpoint Configuration
# When running your Python backend (e.g. Flask or FastAPI), configure its URL here:
REACT_APP_API_BASE_URL=http://127.0.0.1:5000

# Note: You can also change the API Base URL at runtime directly inside the UI!
# Click the 'API Status' or 'Settings' icon in the top navbar or visit the Profile page.
